{
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python",
      "config": {
        "requirements": "requirements.txt",
        "installCommand": "apt-get update && apt-get install -y libsqlite3-dev"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ]
}
